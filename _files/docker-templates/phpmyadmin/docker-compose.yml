services:
  phpmyadmin:
    image: phpmyadmin:5
    container_name: phpmyadmin
    environment:
      - PMA_ARBITRARY=0
      - PMA_HOST=mariadb
    ports:
      - 8080:80
    restart: always
    depends_on:
      - mariadb
  mariadb:
    image: mariadb:12.2
    container_name: mariadb
    volumes:
      - './db:/var/lib/mysql:Z'
    ports:
      - 127.0.0.1:3306:3306
    restart: always
#    environment:
#      - MARIADB_ROOT_PASSWORD=linuxserver
